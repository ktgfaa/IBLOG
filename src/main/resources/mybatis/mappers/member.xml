<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.member">

	<select id="idsearch" parameterType="String" resultType="String">
		SELECT
		id FROM iblog_member
		WHERE
		email = #{email}
	</select>
	
	<update id="updatePwd" parameterType="java.util.Map">
		<![CDATA[
			update iblog_member set pwd=#{pwd} where id=#{userid}
		]]>
	</update>

	<select id="selectPwd" resultType="String">
		<![CDATA[
			select pwd from iblog_member where id=#{id}
		]]>
	</select>

	<insert id="insertMember" parameterType="memberVO">
		<![CDATA[
			insert into iblog_member(id,pwd,username,email) values(#{id},#{pwd},#{username},#{email})
		]]>
	</insert>

	<update id="updateMember" parameterType="memberVO">
		<![CDATA[
			update iblog_member set pwd=#{pwd}, username=#{username}, email=#{email} where id=#{id}
		]]>
	</update>

	<delete id="deleteMember" parameterType="String">
		<![CDATA[
			delete from t_member where id=#{id}
		]]>
	</delete>
	
	<select id="loginById" resultType="memberVO" parameterType="memberVO">
		<![CDATA[
			select * from iblog_member where id = #{id} and pwd = #{pwd}
		]]>
	</select>
	
	<select id="idcheck" parameterType="String" resultType="Integer">
		SELECT
		COUNT(*) FROM iblog_member
		WHERE
		id = #{id}
	</select>
	
	<select id="emailcheck" parameterType="String" resultType="Integer">
		SELECT
		COUNT(*) FROM iblog_member
		WHERE
		email = #{email}
	</select>

</mapper>